
# ==========================================================
# Various system control settings
# ==========================================================

# Toggle trackpad
bindsym $mod+Shift+Menu exec --no-startup-id sh ~/.local/bin/toggletrackpad

# Toggle i3bar
bindsym $mod+Menu bar mode toggle bar1
bindsym $mod2+Menu bar mode toggle bar2

# Backlights
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 4 -steps 1 && pkill -RTMIN+2 i3blocks
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 8 -steps 1 && pkill -RTMIN+2 i3blocks
bindsym Shift+XF86MonBrightnessDown exec --no-startup-id xbacklight -set 0 -steps 8 && pkill -RTMIN+2 i3blocks
bindsym Shift+XF86MonBrightnessUp exec --no-startup-id xbacklight -set 100 && pkill -RTMIN+2 i3blocks
bindsym XF86BrightnessAdjust exec i3-input -F 'exec --no-startup-id xbacklight -set %s' -P 'Set backlight to:'

# ----------------
# LOGIN AND POWER:
# ----------------

# F1: Reload or restart i3 inplace
bindsym $mod+F1 exec --no-startup-id cat ~/.config/i3/conf.d/*  > ~/.config/i3/config; reload
bindsym $mod+Shift+F1 exec --no-startup-id cat ~/.config/i3/conf.d/*  > ~/.config/i3/config; restart

# F2: lock/leave session
bindsym $mod+F2 exec --no-startup-id sh -c ~/.local/bin/scrotlock
bindsym $mod+Shift+F2 exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# F3: suspend/hybrid-sleep
bindsym $mod+F3 exec --no-startup-id sh -c ~/.local/bin/scrotlock && systemctl suspend
bindsym XF86Sleep exec --no-startup-id sh -c ~/.local/bin/scrotlock && systemctl suspend
bindsym $mod+Shift+F3 exec --no-startup-id sh -c ~/.local/bin/scrotlock && systemctl hybrid-sleep
bindsym Shift+XF86Sleep exec --no-startup-id sh -c ~/.local/bin/scrotlock && systemctl hybrid-sleep

# Poweroff
bindsym $mod+F4 exec "i3-nagbar -t warning -m 'Power down your computer?' -b 'Hibernate' 'systemctl hibernate' -b 'Poweroff' 'systemctl poweroff' -b 'Reboot' 'systemctl reboot'"

# xkill
bindsym --release $mod+Shift+Escape exec xkill
